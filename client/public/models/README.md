Place any .glb 3D model files here if you want HeroScene.jsx to load a
custom model instead of (or alongside) the procedural constellation network
it renders by default. No external assets are required for the app to work.
