# URL of your Node/Express backend API (see server/ folder)
VITE_API_URL=http://localhost:5000/api

# Never put secret keys (Gemini, Mongo URI) here. This file is bundled into
# client-side JS and is publicly visible in the browser. Secrets belong in
# server/.env and ai-service/.env only.
