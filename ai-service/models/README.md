This app calls the Gemini API in the cloud, so there are no local model
weights to store here. This folder is kept for parity with the original
project structure - if you ever add a local embedding model (e.g. for a
smarter recommender.py) this is where its files would live.
